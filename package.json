{
  "name": "discord-web-manage",
  "version": "0.1.0",
  "private": true,
  "description": "Discord bot web management — monorepo root",
  "workspaces": [
    "frontend",
    "serverend"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,web -c cyan,magenta --raw \"npm run dev -w serverend\" \"node scripts/wait-for-port.mjs 3000 && npm run dev -w frontend\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:server": "npm run dev -w serverend",
    "build": "npm run build -w serverend && npm run build -w frontend",
    "lint": "npm run lint -w frontend",
    "start": "npm run start -w serverend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
