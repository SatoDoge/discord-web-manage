services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      PORT: "3000"
    volumes:
      - app-data:/app/serverend/data
    restart: unless-stopped

volumes:
  app-data:
